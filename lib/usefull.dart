import 'package:flutter/material.dart';

const splashColor = Colors.redAccent;

const mainColor = Color.fromARGB(6, 4, 31, 255);
// hex(#06041f)
const buttonColor = Colors.red;
const textColor2 = Colors.white;
const textColor = Color.fromARGB(142, 140, 161, 255);
